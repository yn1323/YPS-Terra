import { ComponentMeta } from '@storybook/react'
import { Layout } from '.'

export default {
  title: 'templates/Layout',
  component: Layout,
} as ComponentMeta<typeof Layout>
export const Basic = {}
