overwrite: true
schema: http://localhost:3000/graphql
documents: 'apps/web/graphql/**/*.ts'
generates:
  apps/web/graphql/generated.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-query
  ./graphql.schema.json:
    plugins:
      - 'introspection'
hooks:
  afterAllFileWrite:
    - prettier --write
